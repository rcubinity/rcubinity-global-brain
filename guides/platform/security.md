# Security (baseline)

- Validate and sanitize all external input.
- Enforce authn/authz server-side, not only in UI.
- Use secure credential hashing and avoid logging secrets.
- Enforce HTTPS in production; use least-privilege service accounts.
- Keep dependencies scanned and updated.
