# Styling guidelines

- Keep tokens/theme variables as the source of truth for colors, spacing, and typography.
- Avoid one-off hardcoded values when theme tokens exist.
- Keep responsive behavior intentional and consistent across breakpoints.
