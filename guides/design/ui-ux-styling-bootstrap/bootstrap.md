# Bootstrap usage guidelines

- Use Bootstrap for layout/utilities where the project already relies on it.
- Do not mix competing layout systems in the same feature without a migration plan.
- Keep overrides minimal and centralized to avoid style drift.
